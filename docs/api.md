# API 文档说明

旧版 `docs/api.md` 曾经包含早期接口草稿，其中有认证、周边查询、独立评论模块等当前主流程并未实际接入的内容。为了避免继续误导，本文件只保留指引。

当前真实运行接口请看：

- `docs/api-runtime.md`

维护 API 文档时，请优先更新 `docs/api-runtime.md`。如果以后恢复登录、周边查询或独立评论模块，再把真实后端实现、前端调用和数据库结构一起补回文档。
